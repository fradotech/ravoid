---
import { SiteConfig } from '@/app/config/site.config';
import { Path } from '@/app/config/path.config';
import NavLinks from './NavLinks.astro';
import NavSearch from './NavSearch.astro';
import NavMobile from './NavMobile.astro';
import DarkModeToggle from '@/infrastructure/components/ui/DarkModeToggle.astro';

const navLinks = [
  { label: 'Home', href: Path.index },
  { label: 'Blog', href: Path.blog.index },
  { label: 'Tags', href: Path.tags.index },
  { label: 'About', href: Path.about },
];

const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 glass-nav">
  <nav class="max-w-6xl mx-auto px-4 h-14 flex items-center justify-between gap-4">
    <a href={Path.index} class="text-lg font-bold tracking-tight flex-shrink-0">
      <span>{SiteConfig.name}</span>
    </a>

    <NavSearch />

    <div class="hidden md:flex items-center gap-1">
      <NavLinks links={navLinks} currentPath={currentPath} />
      <div class="ml-1">
        <DarkModeToggle />
      </div>
    </div>

    <div class="flex items-center gap-1 md:hidden">
      <DarkModeToggle />
      <button
        id="mobile-menu-toggle"
        type="button"
        aria-label="Toggle menu"
        class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-white/[0.06] transition-colors"
      >
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </nav>

  <NavMobile links={navLinks} currentPath={currentPath} />
</header>
